{
  "name": "urbanlens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "城市更新用地智能评估交互系统 — V0.1 interaction prototype",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "esbuild src/city-webgl.js --bundle --minify --format=esm --target=es2022 --outfile=dist/scene.js --legal-comments=eof && esbuild src/atlas-app.js --bundle --minify --format=esm --target=es2022 --outfile=dist/atlas.js --legal-comments=eof",
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "node --check dist/app.js && node --check src/atlas-app.js && node --check src/atlas-style.js && node --check src/geo.js && node --check src/canvas-atlas.js && node --check dist/domain.js && node --check dist/assistant.js && node --check server/worker.js"
  },
  "dependencies": {
    "@mapbox/tiny-sdf": "2.0.7",
    "maplibre-gl": "5.6.1",
    "pbf": "3.3.0",
    "three": "0.180.0"
  },
  "devDependencies": {
    "esbuild": "0.25.9",
    "osmtogeojson": "3.0.0-beta.5"
  },
  "overrides": {
    "@xmldom/xmldom": "^0.8.11"
  }
}
