{
  "name": "urbanlens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "城市更新用地智能评估交互系统 — V0.1 interaction prototype",
  "engines": { "node": ">=22" },
  "scripts": { "start": "node scripts/serve.mjs", "test": "node --test", "check": "node --check dist/app.js && node --check dist/domain.js" }
}
